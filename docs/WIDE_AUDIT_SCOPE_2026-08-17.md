# نطاق التدقيق الموسع — 17 أغسطس 2026

سيُنفذ التدقيق على المسارات المستقلة التالية ثم تُدمج النتائج قبل أي تعديل:

| المسار | نطاق الفحص | معيار النجاح |
|---|---|---|
| جودة المصدر | الأنواع، ESLint، الاختبارات، البناء، والاعتماديات | نجاح الأوامر وغياب التحذيرات أو الأعطال المؤكدة. |
| الواجهات وطبقة الأمان | التحقق من المدخلات، المصادقة، معالجة الأخطاء، الرؤوس الأمنية، ومفاتيح إعادة المحاولة | رفض الطلبات غير المصرح بها أو غير الصالحة بلا أثر جانبي. |
| قاعدة البيانات | مخطط البيانات، RLS، التنبيهات، والمنطق التشغيلي المتاح للقراءة | عدم وجود خلل موثق أو تنبيه حرج غير معالج. |
| تجربة المستخدم | العربية والإنجليزية، البحث، الحالات الفارغة، الحماية، والاستجابة | انتقالات سليمة ورسائل دقيقة وعدم ظهور بيانات اختبارية. |
| PWA والإنتاج | عامل الخدمة، عدم الاتصال، صحة النشر، وسجل أعطال وقت التشغيل | اختبار إنتاج ناجح وحالة نشر جاهزة بلا أخطاء تشغيلية حديثة. |

> لا يتضمن النطاق إنشاء حجوزات أو حسابات أو عروض عامة مصطنعة. تُختبر هذه المسارات فقط عند توفر حساب مرخّص وبيانات تشغيلية معتمدة، حتى لا يُفسد اختبار الجودة بيانات المستخدمين أو السوق.
