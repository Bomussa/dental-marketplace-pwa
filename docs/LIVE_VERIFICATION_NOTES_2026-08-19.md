# ملاحظات التحقق التشغيلي الحي — 19 أغسطس 2026

## فحص الصحة

- الوقت: 15:05:40 UTC تقريباً وفق استجابة الخادم.
- الرابط: `https://www.mmc-mms.com/api/health`.
- النتيجة: `ok=true`، و`database=true`، و`server_operations=true`، و`treatments=48`.
- نوع الفحص: قراءة عامة فقط؛ لم يرسل أي نموذج أو يكتب بيانات أو ينشئ حجزاً.

## تكافؤ النطاقين

أعاد `https://mmc-mms.com/api/health` التوجيه إلى `https://www.mmc-mms.com/api/health` ثم أعاد `ok=true` و`database=true` و`server_operations=true` و`treatments=48` عند 15:06:01 UTC تقريباً. الفحص قراءة عامة فقط ولم يكتب أي بيانات.

## رؤوس النقل والحماية

أعاد النطاق المجرد تحويل `308` إلى `https://www.mmc-mms.com/` عبر HTTPS. أعادت الصفحة الرئيسية ونقطة الصحة رؤوس HSTS لمدة 63,072,000 ثانية وCSP مقيداً بالمصادر الأولى وSupabase فقط، مع `X-Frame-Options: DENY` و`X-Content-Type-Options: nosniff` و`Referrer-Policy: strict-origin-when-cross-origin` و`Cross-Origin-Opener-Policy: same-origin`. نقطة الصحة تفرض `Cache-Control: no-store`.

## الواجهة العامة واللغتان

ظهرت الواجهة الإنجليزية ثم العربية بعد تبديل اللغة، مع العلاج والنوع الدقيق وتفضيل الموعد وخيارات نطاق المسافة 1/5/10/25/50 كم ومعايير الترتيب الخمسة. ظهر النص الواضح بأن المقارنة وطلب الحجز مجانيان للمريض وأن الدفع المباشر — إن وجد — يكون لدى العيادة وفق السعر ونطاقه المنشورين. لم يُرسل نموذج ولم يُطلب الموقع ولم تُكتب بيانات.

## رفض العقود والصلاحيات العامة

أعاد البحث بمعرّف نوع غير صالح وتفضيل موعد غير صالح `400` مع `invalid_search`. أعاد الحجز بلا جلسة `401`، ورفض مسار تثبيت الجهاز طلباً من أصل غير موثوق بحالة `403` و`forbidden_origin`، كما رفض تصدير CSV لنشاط الإدارة بلا صلاحية بحالة `403` و`FORBIDDEN`. لم تنفذ هذه الطلبات أي كتابة لأنها فشلت قبل طبقات الطفرة.

## قياس خفيف للاستجابة

خمس قراءات عامة لكل من `/api/health` و`/` أعادت `200` من دون كتابة. تراوحت المدة الكلية المرصودة من بيئة التحقق بين 4.02 و5.71 ثوانٍ، وهي أعلى من هدف الأداء الداخلي وتستلزم تشخيصاً قبل نسبتها إلى التطبيق نفسه؛ القياس لا يفصل زمن شبكة بيئة التحقق أو طبقة Vercel أو تنفيذ التطبيق.

## تفكيك زمن الاستجابة

في قياس تفكيكي لاحق، أعادت نقطة الصحة `200` بزمن DNS قدره 0.03 ثانية وبداية استجابة 2.38 ثانية. أعادت الصفحة الرئيسية `200` بزمن DNS قدره 0.02 ثانية وبداية استجابة 5.58 ثانية، مع تأخر كبير مرصود في مرحلة TLS من بيئة التحقق. لذلك لا يكفي هذا القياس وحده لتشخيص مشكلة برمجية أو ادعاء سرعة ثابتة؛ يلزم قياس من مراقبة متعددة المناطق أو بيانات Vercel عند توفر صلاحيتها.

## تبعيات الإنتاج

نجح `npm audit --omit=dev` في نسخة العمل الحالية وأعاد `0 vulnerabilities`.

## الاستضافة وأخطاء التشغيل

أظهر اتصال الاستضافة المصرح به أن الإيداع `4f9859a` نُشر إلى الإنتاج في Vercel بحالة `READY` كأحدث نشر. لم يُظهر فحص مجموعات أخطاء التشغيل خلال آخر 24 ساعة أي أخطاء runtime. صحح الاستعلام معرف الفريق التشغيلي إلى `team_aFtFTvzgabqENB5bOxn4SiO7`؛ كان المعرف القديم في ملخص العمل يحتوي ترتيب حروف غير مطابق.


## تحقق حي عملي إضافي

| المسار | النتيجة المرئية والوظيفية | الحالة |
|---|---|---|
| الصفحة الرئيسية العربية | ظهرت هوية MMC-MMS وقائمة العلاج والنوع الدقيق وتفضيل الموعد ونطاق المسافة 1/5/10/25/50 كم ومعايير الفرز، مع إعلان مجانيّة المقارنة وطلب الحجز للمريض. | ناجح |
| تبديل اللغة إلى الإنجليزية | تبدلت عناصر التنقل والعنوان وحقول البحث والرسالة التعريفية إلى الإنجليزية في الصفحة الحية من دون انتقال خارج النطاق. | ناجح |
| رحلة بحث إنجليزية حيّة | اختير نطاق 25 كم ثم أُرسل بحث لتبييض داخل العيادة. حفظ رابط النتائج `when=earliest` و`radius=25` و`sort=balanced`، وظهرت حالة صفر نتائج تشرح عدم توفر عرض نشط ولا تدّعي عدم توفر الخدمة في قطر. | ناجح |
| مصفوفة عقود وحماية حية | بحث صالح لنوع تبييض داخل العيادة ونطاق 25 كم أعاد `200` مع `count=0` بلا عروض تجريبية. نصف قطر 250 كم رُفض `400 invalid_search`، وGET للتحليلات رُفض `405`، وتسجيل الخروج/تثبيت الجهاز من أصل خارجي رُفضا `403 forbidden_origin`، والحجز بلا جلسة رُفض `401`. | ناجح؛ لا كتابة |
| جلسة اختبار المريض | فتحت `/account` بجلسة اختبار موجودة، وظهرت بيانات QA Clinic 01 (Test) وسجل حجوزات فارغ وتحقق هاتف مطلوب قبل الحجز؛ لم يُحفظ ملف أو تتغير بيانات. | ناجح؛ قراءة فقط |
| جلسة اختبار مالك العيادة | فتحت `/clinic` بالحساب الاختباري، وظهرت لوحة مركز الشامي الطبي (تجريبية) بدور Owner، تقرير النشاط المقيد بالعيادة وتحديثات Realtime، ومقاعد المشغلين والعروض والمواعيد؛ لم يُنفذ حفظ أو تعديل أو إنشاء. | ناجح؛ قراءة فقط |
| تقرير نشاط العيادة المباشر | غُيّر التجميع إلى Monthly ثم حُدّث التقرير. حفظ الرابط المدة والتجميع، وعرض يوليو/أغسطس فقط ونطاق العيادة دون بيانات تعريفية للمرضى؛ لم تُنشأ أو تعدل بيانات. | ناجح؛ قراءة فقط |
| حاجز الإدارة مع جلسة مالك العيادة الاختبارية | الانتقال إلى `/admin` أعاد المستخدم إلى الصفحة الرئيسية ولم يعرض لوحة الإدارة أو أدواتها. | ناجح؛ الفصل بين دور العيادة والإدارة مثبت حياً |
| مراقبة Vercel | فحص مجموعات أخطاء التشغيل خلال آخر 24 ساعة لم يجد أخطاء runtime للمشروع. | ناجح |
| مراقبة Supabase | مصادر السجل الحية متاحة. تعذر تجميع رسائل الخطأ بخطأ خلفي في أداة السجل نفسها، لذلك لا يُنسب كتطبيق. تدقيق الأمن أعاد تحذيرًا خارجيًا واحدًا: حماية كلمات المرور المسرّبة معطلة في Supabase Auth. | قيد مزود معروف؛ لا إصلاح مخزني آمن تلقائي |
| اختيار عيادة غير مخولة عبر الرابط | فُتح `/clinic` بمعرّف عيادة تجريبية أخرى ضمن جلسة مالك اختبار. بقيت الواجهة على مركز الشامي الطبي (تجريبية) المصرح به فقط، ولم تظهر بيانات العيادة المطلوبة أو تتغير بيانات. | ناجح؛ عزل العيادات مثبت حياً |
| عقود الواجهات العامة — إدخال غير صالح | `patient-booking-registration` أعاد 400؛ `book` وبدء/تأكيد تحقق الهاتف و`support` أعادت 401 قبل أي كتابة أو إرسال؛ `device-installations` أعاد 400؛ و`search?radius_km=0` أعاد 400. | ناجح؛ لا إنشاء بيانات أو رسائل |
| صحة الخدمة اللاحقة | `/api/health` أعاد 200 مع `database:true` و`server_operations:true` و`treatments:48`. | ناجح |
| وحدة تحكم المتصفح | لا توجد رسائل أو أخطاء في وحدة تحكم صفحة لوحة العيادة بعد اختبار اختيار عيادة غير مخولة. | ناجح |
| صفحة الخطأ التشغيلي | فُتحت `operation-error` برمز غير معروف؛ عرضت رسالة عامة آمنة وخيار عودة واضحاً، ولم تعرض الرمز المرسل أو مكدس خطأ أو تفاصيل قاعدة بيانات. | ناجح |
| PWA وعامل الخدمة المنشور | أعاد `manifest.webmanifest` حالة 200 وهوية أسناني قطر وأيقونات 192/512 قابلة للاستخدام كقناع. أعاد `sw.js` حالة 200 ويستثني صراحةً `account` و`clinic` و`admin` و`auth` و`api` من وثيقة عدم الاتصال العامة. أظهر المتصفح تسجيلاً نشطاً فعلياً لـ`https://www.mmc-mms.com/sw.js` على نطاق الموقع كله، من دون عامل خدمة بانتظار التنشيط. | ناجح؛ لا تخزين مؤقت لمسارات خاصة |
| واجهة الدخول مع `next` خارجي | فُتحت `/login?next=//evil.example` وبقيت داخل نطاق الموقع مع نموذج اسم المستخدم وكلمة المرور؛ لم يحدث انتقال فوري لخارج النطاق. | ناجح؛ واجهة دخول آمنة |
| تحقق نموذج الدخول المحلي | محاولة إرسال حقلي اسم المستخدم وكلمة المرور فارغين أوقفتها قواعد المتصفح على الحقل المطلوب قبل إرسال أي طلب دخول. | ناجح؛ لا محاولة مصادقة غير لازمة |
| رحلة الحساب المصدقة اللاحقة | فُتحت `/account` بجلسة اختبار المريض الحالية، وظهرت لوحة QA Clinic 01 (Test) وسجل حجوزات فارغ وحالة التحقق المطلوبة قبل الحجز ونموذج بيانات تابع جديد؛ لم يُنفذ حفظ أو تعديل أو تحقق هاتف. | ناجح؛ قراءة فقط |
| تحقق نموذج ملف تابع | محاولة حفظ النموذج الفارغ أوقفتها قواعد المتصفح على حقل الاسم المطلوب قبل إرسال أي طلب أو كتابة في قاعدة البيانات. | ناجح؛ لا إنشاء ملف |
| عقد تسجيل حساب المريض داخل جلسة اختبار | أُرسل جسم JSON فارغ فقط إلى `patient-booking-registration`؛ أعاد الخادم 400 ورسالة تحقق من البيانات قبل أي إنشاء حساب أو ملف. | ناجح؛ لا كتابة |
| تدقيق Supabase الأمني | لم يظهر سوى تحذير خارجي معلوم: تعطيل حماية كلمات المرور المسرّبة في Supabase Auth؛ لم تُسجل ملاحظات عن RLS مفقود أو كشف صفوف من تدقيق الأمن. | قيد مزود معروف؛ لا تغيير تلقائي |
| تدقيق RLS المخزني | قورنت سياسات الإنتاج الوصفية بترحيلات المشروع: قراءة الحجز مقيدة بصاحب الحجز أو وصول الفرع، وقراءة ملف المريض مقيدة بالحساب نفسه أو بدور تشغيلي مخول للحجوزات المرتبطة فقط. | ناجح؛ لا قراءة صفوف أعمال |
| تدقيق الأداء المخزني | ظهرت ملاحظات معلوماتية لفهارس غير مستخدمة بعد على بيانات تجريبية، وتحذيرات تكرار سياسات متساهلة تجمع دور الإدارة مع أدوار التشغيل. لا تمثل دليلاً على خلل وظيفي أو حذف فهرس آمن؛ لا تغيير قاعدة بيانات تلقائي. | متابعة عند توفر حمل تشغيلي ممثل |
| منح تنفيذ دوال الحجز | دالة الحجز الخادمية وواجهات الحجز المباشرة لا تملك منح تنفيذ لدور `authenticated` أو `anon`؛ التنفيذ محصور بـ`service_role`. إسقاط تفاصيل المريض موجه لـ`authenticated` لكنه مقيد داخلياً بسياسة وصول الفرع التشغيلي. | ناجح؛ فصل الخادم والأدوار مثبت مخزنياً |
| منح إلغاء الحجز | دالة `cancel_booking_server` لا تملك منح تنفيذ لدور `authenticated` أو `anon`، وحصرها الإنتاجي هو `service_role` والخادم الإداري فقط. | ناجح؛ لا مسار إلغاء مباشر من المتصفح |
| اختبارات الوحدات اللاحقة | أعاد `npm run test` نجاح 15 ملف اختبار و64 اختباراً، بما فيها عقود الحجز والتحقق وRealtime والتقارير. | ناجح |
| ظهور العرض التجريبي في البحث العام | العرض والموعد التجريبيان النشطان يحققان شروط السعر والاعتماد والتوفر، لكن العيادة موسومة `is_synthetic=true` وتُستثنى صراحةً من البحث العام؛ أعاد البحث العام بالنوع الدقيق `200` و`count=0` كما صُمم. | ناجح؛ حماية من تسريب بيانات الاختبار |
| عروض عامة مؤهلة غير تجريبية | لم يظهر أي عرض نشط مؤهل وغير تجريبي في الاستعلام المقيد، ولذلك لا تتوفر حالياً حالة إنتاج آمنة لاختبار بطاقة مقارنة وحجز قبل الدخول من البداية إلى النهاية. لم تُنشأ بيانات جديدة لهذا الغرض. | قيد تغطية حي؛ لا خلل برمجي مثبت |
| واجهة المقارنة مع عرض تجريبي مستثنى | فتحت نتائج النوع الدقيق `Root Canal — Molar` وأظهرت 0 نتائج ورسالة دقيقة لا تنفي توفر الخدمة في قطر، ولم تعرض أي اسم عيادة أو عرض تجريبي. | ناجح؛ حماية بيانات الاختبار وتجربة عدم توفر آمنة |
| اختبارات البحث والترتيب | أعاد اختبارا عقود البحث والترتيب 19 اختباراً ناجحاً، بما فيها حفظ معلمات البحث ومنع تفضيل موعد غير صالح والفصل بين الفرز المتوازن وفرز السعر. | ناجح |
