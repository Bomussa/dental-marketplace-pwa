# تحقق إصلاح تسجيل PWA — 18 أغسطس 2026

تم تشخيص العطل على معاينة Vercel: كان تسجيل العامل يبدأ ثم ينتقل من `installing` إلى `redundant` لأن طلب التخزين المسبق لـ`/offline.html` استخدم `credentials: "omit"`، وهو وضع يعيد `Failed to fetch` في بيئة النشر. أزيل هذا الخيار ليستخدم الطلب اعتماداته الافتراضية، مع بقاء التسجيل في `components/locale-toggle.tsx`، وهو مكون عميل تفاعلي موجود في الرأس لكل صفحة. لا يضيف الإصلاح أي عنصر مرئي أو تفاعل جديد.

| بند التحقق | النتيجة | الدليل |
| --- | --- | --- |
| تحميل الصفحة الرئيسية من بناء إنتاجي محلي | ناجح | ظهر نموذج البحث والهوية الجديدة بصورة سليمة على `http://127.0.0.1:3000/` |
| تسجيل عامل الخدمة | ناجح | فحص المتصفح بعد ربط التسجيل بمفتاح اللغة أعاد نطاقًا فعالًا `http://127.0.0.1:3000/` وحالة `active: true` و`controller: true` |
| عدم تحويل طلب API غير متصل إلى HTML مخزن | ناجح | اختبار Playwright المتسلسل اجتاز |
| عرض صفحة عامة ثابتة عند فقد الاتصال | ناجح | اختبار Playwright المتسلسل اجتاز |
| حزمة متصفح كاملة على سطح المكتب والهاتف | ناجح | اجتازت 40 حالة اختبار في 42.0 ثانية من خادم بناء جديد |

> هذا التحقق لا ينشئ حجزًا ولا يرسل رسالة ولا يلمس بيانات مريض. اجتازت حزمة الاختبارات الكاملة؛ وما يزال النشر على المعاينة والإنتاج مطلوبًا قبل اعتماد النتيجة النهائية.

## المراجع

[1]: ./evidence/visual-redesign-preview/home-en.webp "دليل معاينة الهوية البصرية"
[2]: ../components/locale-toggle.tsx "مفتاح اللغة الذي يسجل PWA"
[3]: ../public/sw.js "عامل الخدمة العام وإصلاح التخزين المسبق"
