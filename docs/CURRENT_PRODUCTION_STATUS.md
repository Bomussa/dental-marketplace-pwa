# الحالة الإنتاجية الحالية — أسناني قطر / MMC-MMS

**آخر مراجعة موثقة:** 26 أغسطس 2026. الإصلاح الوظيفي لمساحة العميل التشغيلي وحلقة RLS موجود في `e7bc1a22b5fcd3f75affb2044ac8b047ea79e90f`، ثم نُشرت مواءمة مصدر وترحيل الحارس الأمامي في `3363e0f24d3c574ef48c8cf82582f878f1826bad` عبر نشر Vercel `dpl_J9fU9p2cQQsScHCfm8BLsJyVy5W5` بحالة `READY`. سجل Supabase Production ترحيلات RLS الثلاثة تحت `20260826194116` و`20260826194133` و`20260826194534` بعد نجاحها في Staging؛ لا تتضمن DML أعمال أو بيانات اختبار. يقرأ هذا الملف مع [README الجذري](../README.md)، وهو **سجل الإصدار والحالة الحالية**؛ أما التقارير المؤرخة الأقدم فتبقى أدلة تاريخية ضمن نطاقها الزمني.

> **حكم البوابة الحالي:** **NOT READY — RELEASE BLOCKERS REMAIN.** نُشرت تقوية الصلاحيات والإصلاحات المثبتة بنجاح، وثُبتت ميزات تجربة المريض الجديدة على Preview Staging بحساب وعيادة وعرض وموعد `TEST_STG` مع تنظيف صفري لاحق. لكن لا يجوز إعلان جاهزية حجز عامة كاملة ما دام OTP/SMS الحقيقي غير مهيأ؛ تحقق الهاتف في الاختبار كان اصطناعيًا ومحصورًا في Staging ولا يثبت إرسال رمز فعلي. ولم ينفذ اختبار K6 الفعلي للحجز/إعادة المحاولة لأن Staging يعود خاليًا من أي بيانات تشغيلية بعد التنظيف. لا تُنشأ تفعيلات أو مواعيد اصطناعية لتجاوز ضوابط التحقق، ولا تُنشأ بيانات تشغيلية اصطناعية في Production.

## جولة إغلاق الفجوات — 26 أغسطس 2026

نُفذ الالتزام `a577d8a4996f77f22a8e95b760bf7596f369645c` ثم تكملته `decf85cda5568a3a6247c774d5e6d3280fc90ea2` على فرع `staging` فقط عبر Preview المحمي `dpl_5s5SgGFJa2SXCJjFLFMuShcd8b5U` بحالة `READY`. أضافت الجولة بوابة جاهزية إنتاجية قراءة فقط متوافقة مع عقد الصحة والبحث، ومساري `/privacy` و`/terms` كإطار تقني يصرح بأن المحتوى القانوني يحتاج اعتماد المالك/المختص، وفحص وصول آلي WCAG A/AA، وسيناريو K6 محروس لإعادة المحاولة والسباق. لا يمثل الإطار التقني اعتماداً قانونياً، ولا تمثل إضافة السيناريو تنفيذاً لـK6.

اكتشف مستشار أداء Production تحذير `multiple_permissive_policies` على `public.patient_profiles`: كانت سياسة وصول ذاتي مؤرشفة جزئياً تتداخل مع سياسة أقدم تسمح للمالك بقراءة كل ملفاته. أضيفت migration المصدرية `20260826230000_consolidate_patient_profiles_select_policy_v1.sql` وطُبقت **في Staging فقط**؛ دمجت القراءة في سياسة واحدة تشترط أن الملف الذاتي غير مؤرشف وتبقي وصول موظف الفرع المصرح إلى ملفات الحجوزات المرتبطة. لم تطبق هذه migration في Production، ولا يجوز ذلك قبل تأكيد صريح منفصل.

## سجل الإصدار

| البند | القيمة المثبتة |
|---|---|
| المستودع والفرع | `Bomussa/dental-marketplace-pwa`، الفرع `main`. |
| آخر Commit منشور | `3363e0f24d3c574ef48c8cf82582f878f1826bad` — مواءمة مصدر الحارس الداخلي وتوثيق تطبيقه؛ الإصلاح الوظيفي لمساحة العميل وحلقة RLS موجود في `e7bc1a2`. |
| Vercel | المشروع `dental-marketplace-pwa` (`prj_Dj3iqScGPVpMZdbluhw7YUwNBbSr`)؛ نشر Production `dpl_J9fU9p2cQQsScHCfm8BLsJyVy5W5` بحالة `READY` ويطابق `3363e0f`. ظل Preview Staging المحمي `dpl_3FucKh3XRZ7GVqm96Tfystrt9F5k` دليل إصلاح قراءة الحجوزات المقيدة ويطابق `d759be3`. |
| النطاقات | [www.mmc-mms.com](https://www.mmc-mms.com) و[mmc-mms.com](https://mmc-mms.com). |
| Supabase Production | `bqvcukxfsnchvkgejolz`؛ طابق عداد الكتالوج الصفحة العامة: 48 علاجًا رئيسيًا و79 نوعًا دقيقًا. |
| Staging المنفصل | `yrlwoxlxizxgrodtbdcp`؛ استُخدم لاختبارات الكتابة والـfixtures ثم نُظف، ولم تُنقل أي بيانات منه إلى Production. |
| هدف الرجوع | نشر Production السابق `dpl_2HXL8qSfRKfCXJzaaP8hZdZFp8jt` هو rollback candidate المعروف. إعادة توجيه النشر لا تعكس DDL؛ أي تغيير مخطط لاحق يحتاج migration مصدرية أمامية. |

## التغييرات المنشورة ولماذا

| التغيير | السبب والأثر | حالة التحقق |
|---|---|---|
| حصر تنفيذ RPC | سُحب تنفيذ كل دوال `public` من `PUBLIC` و`anon` و`authenticated`، ثم أُعيد منح أقل صلاحيات لدوال الواجهة المقصودة فقط. الدوال ذات اللاحقة `_server` بقيت لـ`service_role` فقط. | **PASS**: بعد التطبيق كان `anon_server_execute=0` و`authenticated_server_execute=0` و`service_server_execute=23`. |
| البحث العام والموثق | بقي `search_dental_offers` ومساعد نطاق السعر غير الخادمي متاحين للزوار، وبقي البحث نفسه متاحًا للمستخدم المسجل. | **PASS**: تحقق grants حي وصفحة `/results` عامة ناجحة بلا كتابة. |
| قابلية إعادة بناء المخطط | أضيف baseline خالٍ من البيانات مع مولدي/متحقق كتالوج، ووثق تسلسل baseline ثم مهاجرات التقوية الخمس. | **PASS في Staging محلي معزول**: تطابق 49 جدولًا و75 دالة و321 قيدًا وRLS/policies/indexes/triggers المرجعية، مع postconditions للتقوية. |
| cache كتالوج العلاج | قُصّرت إعادة التحقق إلى 60 ثانية وأُبطل مفتاح cache السابق كي لا تبقى بيانات fixtures المحذوفة ظاهرة في Preview. | **PASS** في Preview Staging بعد تنظيف TEST_STG. |
| اختبار اتصال Supabase المحلي | أصبح اختبار الاتصال الخارجي مشروطًا بتوفر متغيري public الفعليين؛ لا يضع أي مفتاح في shell أو Git، لكنه يبقى فعالًا عند توفرهما. | **PASS**: `npm run verify` ينجح مع تخطي الاختبار غير المهيأ محليًا. |
| تسجيل المريض والهاتف | نُقلت الجنسية إلى مصدر ISO 3166-1 alpha-2 موحد يضم 249 خيارًا، وأصبحت الحقول الأساسية الاسم والرقم الشخصي والجنسية والهاتف وبيانات الدخول. تاريخ الميلاد والجنس اختياريان عند إنشاء الحساب، مع بقاء تاريخ الميلاد مطلوبًا قبل تأكيد الحجز. صارت كلمة مرور المريض 4–10 أحرف أو أرقام إنجليزية، وسياسة حسابات العيادات والإدارة مستقلة. ثُبّتت CSS الهاتف لمنع تجاوز عرض الوثيقة أو صفوف البحث. | **PASS**: Preview Staging أنشأ حساب TEST_STG واحدًا بكلمة مرور من 4 أحرف عبر الواجهة الحقيقية ثم حُذف؛ فحص النظافة اللاحق أعاد صفر مستخدمين وملفات وأسماء مستخدمين للاختبار. اختبار متصفح محلي معزول بعرض 393px أعاد `scrollWidth=clientWidth=393` بلا تجاوز أفقي للصفحة الرئيسية أو صفحة الدخول، وProduction عرض الحقول الجديدة و`/api/health` بنجاح قراءةً فقط. |

| تجربة المريض المنشورة | يفرض migration `20260826100000_patient_experience_integrity_v1.sql` تفرد هاتف المريض، ويضيف nickname مريض 2–10، جنس الطبيب والبحث المفلتر، قناة إشعار `in_app` وتفاصيل الموقع، والحذف الذاتي المؤرشف المقيد. يضيف migration `20260826103000_patient_account_admin_management_v1.sql` قائمة مرضى دنيا للإدارة العليا فقط. الواجهات تعرض اتجاهات حقيقية من إحداثيات أو عنوان منشور، وتحمّل الحساب وملف الحجز بمرشح claim خادمي. | **PASS للمخطط/الكود في Production**: طُبق الترحيلان وفحوص postconditions المقروءة اجتازت. **PASS وظيفي في Staging فقط**: سجل TEST_STG عبر الواجهة، حجز عرضًا لطبيبة، ظهر إشعار مؤكد محفوظ مع المكان والوقت ورابط الخريطة وتنبيه 30 دقيقة، ثم أثبت الحذف الذاتي وسجل الخروج. أُزيل الحساب والحجز والإشعار والعيادة والعرض والموعد وكل آثار TEST_STG بفحص عدادات صفرية. لم يرسل SMS للحجز. |

## ترحيلات Production المطبقة في هذا الإصدار

لا تتضمن هذه الترحيلات حذف بيانات أو نقلًا بين البيئات أو إنشاء حسابات/حجوزات/مواعيد. تحفظ الملفات المصدرية ضمن `supabase/migrations/`، وقد سجلتها Supabase بالأرقام التالية عند التطبيق في Production.

| ملف المصدر | الإصدار المسجل في Production | الغرض |
|---|---:|---|
| `20260825191000_harden_public_function_execute.sql` | `20260825201107` | حصر تنفيذ الدوال وخدمات RPC الخادمية. |
| `20260825192500_revoke_anon_server_rpc_execute.sql` | `20260825201142` | إزالة منح anon الموروثة وإبقاء بحث الزوار. |
| `20260825193500_limit_authenticated_security_definer_rpc.sql` | `20260825201217` | منح authenticated لدوال الواجهة المحددة فقط. |
| `20260825194500_allow_public_search_validation_helper.sql` | `20260825201253` | منح مساعد نطاق السعر غير الخادمي للبحث العام فقط. |
| `20260825195000_allow_authenticated_search_rpc.sql` | `20260825201329` | إبقاء بحث القراءة فقط متاحًا للمستخدم المسجل. |
| `20260826100000_patient_experience_integrity_v1.sql` | `20260826122625` | تجربة المريض: تفرد الهاتف وnickname وجنس الممارس وإشعارات الحجز الداخلية وأرشفة الحساب. |
| `20260826103000_patient_account_admin_management_v1.sql` | `20260826122705` | جرد أدنى لحسابات المرضى للإدارة العليا في المسار الخادمي. |
| `20260826130000_notification_outbox_rls_initplan_v1.sql` | `20260826163826` | تحسين سياسة قراءة إشعارات الحساب بإعادة استخدام claims الطلب نفسه بدل إعادة تقييم JWT لكل صف. |
| `20260826193000_booking_patient_rls_recursion_fix_v1.sql` | `20260826194116` | كسر حلقة RLS بين قراءة الحجوزات وملفات المرضى مع بقاء حارس الحساب المؤرشف الذاتي. |
| `20260826193500_booking_patient_rls_helper_execute_grant_v1.sql` | `20260826194133` | تسجيل منح تنفيذ الحارس الداخلي لدور `authenticated` فقط. |
| `20260826194500_booking_patient_rls_helper_search_path_v1.sql` | `20260826194534` | تثبيت مسار بحث `pg_catalog` للحارس الداخلي مع بقاء جداول التطبيق مؤهلة بالـschema. |

## التحقق الفعلي

| المجال | النتيجة |
|---|---|
| فحص المصدر | `npm run verify`: فحص predeploy وTypeScript وlint والبناء نجحت؛ **103** اختبارات Vitest ناجحة واختبارا اتصال public متخطيان عمداً عند غياب تشغيل الشبكة الصريح محلياً. أزيلت تحذيرات default exports من سيناريوهات K6؛ لا أخطاء ولا تحذيرات lint حالياً. |
| اختبار Browser المحلي وStaging | **BLOCKED محليًا**: `npm run test:e2e` يحتاج URL ومفتاح Supabase public محليين؛ لم تُنسخ قيم Production أو Staging إلى shell. **PASS في Preview Staging**: سجل دخول العميل التشغيلي الحقيقي بالنطاق `clinic_bookings_only`، ورأى فرعه Alpha فقط وحجزين مصرحين. نجحت انتقالات pending → confirmed → checked-in → completed. أعادت محاولات `/` و`/account` و`/admin` و`/clinic` إلى مساحة الحجوزات وفق حاجز proxy المركزي. نُظفت جميع fixtures بعدها بعدادات صفرية موسعة. |
| صفحة Production الرئيسية | **PASS**: HTTPS والواجهة العربية والكتالوج العام حملا بنجاح بعد النشر، مع 48/79 من بيانات Production القائمة. |
| البحث والنتائج | **PASS**: صفحة نتائج عامة لنوع علاجي قائم استجابت بلا خطأ صلاحيات؛ عدم وجود نتيجة لهذا النوع لا يمثل خللًا بحد ذاته ولا يبرر إنشاء موعد اصطناعي. |
| الصحة | **PASS**: `/api/health` أعاد `{ "ok": true }` في فحص قراءة فقط بعد نشر `3363e0f`. |
| التسجيل والواجهة الهاتفية | **PASS**: Production عرض قائمة الجنسية الكاملة، ووسم الحقول الاختيارية، وقيد كلمة المرور 4–10. أُثبت التسجيل الحقيقي في Preview Staging بحساب TEST_STG قصير كلمة المرور ثم نُظف؛ وأثبت اختبار عرض 393px محليًا غياب تجاوز المستند أفقيًا. |
| ميزات تجربة المريض المنشورة | **PASS للمخطط/الكود في Production**: تم تسجيل الترحيلين وفحوص postconditions المقروءة، وحملت الصفحة الرئيسية خيار طبيبة/طبيب وتبديل اللغة الفوري في فحص قراءة فقط. **PASS وظيفي في Staging فقط**: nickname 2–10 وفريد، منع تكرار الهاتف، فلترة الطبيب/الطبيبة، رابط الاتجاهات، الحجز، إشعار التأكيد الداخلي المحفوظ، وملاحظة الحضور قبل 30 دقيقة، وتحميل الحساب بعد الحجز، والحذف الذاتي. تحقق الحساب/الحجز تم بfixture TEST_STG ثم أعاد فحص النظافة صفراً لجميع العدادات المحددة. |
| Console | **PASS**: لا توجد رسائل Console في الفحص النهائي. |
| أخطاء تشغيل Vercel | **PASS في فحص ما بعد النشر**: لم تظهر أخطاء أو أخطاء قاتلة في نافذة 10 دقائق للنشر `dpl_J9fU9p2cQQsScHCfm8BLsJyVy5W5` بعد وصوله إلى `READY` وفحص `/api/health` والصفحة العامة قراءةً فقط. لا يثبت ذلك اختبار الحجز أو OTP أو الحمل في Production. |
| مستشار الأمان | **WARN واحد**: بقي فقط `auth_leaked_password_protection` (حماية كلمات المرور المسرّبة معطلة). [إرشاد Supabase](https://supabase.com/docs/guides/auth/password-security#password-strength-and-leaked-password-protection). لم تُفعّل ترقية أو تكلفة تلقائية، ولا توجد في نتيجة المستشار تحذيرات SECURITY DEFINER أو grants عامة. |
| مستشار الأداء | **WARN واحد وINFO متعددة**: أزيل تحذير `auth_rls_initplan` من سياسة `public.notification_outbox` سابقاً. كشف الفحص اللاحق تحذير `multiple_permissive_policies` على `public.patient_profiles`؛ أصلحته migration مصدرية واختبر في Staging فقط ولم تطبق في Production بعد. بقيت ملاحظات فهارس غير مستخدمة؛ لا تحذف تلقائياً لأن عدم الاستخدام المرصود لا يثبت عدم لزومها تشغيلياً. [إرشاد فهارس Supabase](https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index) [إرشاد السياسات المتعددة](https://supabase.com/docs/guides/database/database-linter?lint=0006_multiple_permissive_policies). |

## مصفوفة أدلة جولة الإغلاق

| المجال | الدليل المنفذ | النتيجة الحالية |
|---|---|---|
| بوابة الجاهزية العامة | شغلت `scripts/production-readiness-check.mjs` على Production قراءة فقط: الصفحة، الصحة، manifest، icon، robots، sitemap، الدخول، البحث الصحيح/الفارغ/غير الصالح، والرؤوس الأمنية. | **PASS** لعقد القراءة العام الحالي. Preview Staging محمي عمداً ويعيد 401 لطلبات HTTP غير المصرح بها؛ التحقق المرئي المصرح للمسارات الجديدة نجح. |
| الخصوصية والشروط | اختبرت `/privacy` و`/terms` على Preview Staging؛ تظهران إطار اعتماد صريح وروابط تذييل وتسجيل. | **PASS تقني فقط**؛ النص القانوني النهائي **LEGAL/OWNER DECISION**. |
| الوصول الآلي | أضيف `@axe-core/playwright` واختبار WCAG A/AA للصفحة والدخول. | **IMPLEMENTED**؛ تشغيل E2E الخارجي يبقى محجوباً محلياً بسبب إعدادات Supabase/حماية Preview، لذلك لا يرقى إلى PASS تشغيلي حتى تشغيله في بيئة مصرح لها. |
| RLS والدوال الخادمية | فحص كتالوج Production قراءة فقط: 49 جدولاً عاماً بـRLS، صفر grants لدوال `_server` لـanon/authenticated، ولا SECURITY DEFINER بلا `search_path`. | **PASS محدود للكتالوج**؛ لا يغني عن اختبار IDOR حي بالأدوار. |
| سياسة ملفات المرضى | فحص Staging قبل/بعد migration أظهر انتقالاً من سياستين permissive إلى سياسة واحدة archived-aware مع بقاء وصول موظفي الفرع. | **PASS في Staging**؛ **PENDING USER CONFIRMATION** قبل DDL Production. |
| OTP والحجز/K6 | عدادات Staging أظهرت صفر عيادات/فروع/عروض/مواعيد نشطة وحجوزات `TEST_STG`؛ Twilio غير مهيأ. | **BLOCKED**: لا fixture ولا إرسال OTP حقيقي ولا K6/retry/concurrency حي، ولم تُختلق بيانات أو تجاوزات. |
| الأسرار | فحص ملفات المصدر المتتبعة لم يظهر رمزاً حياً أو JWT أو secret pattern؛ الإشارات المتبقية أسماء متغيرات وترحيلات واختبارات. | **PASS محدود لشجرة المصدر**؛ مراجعة إعدادات النشر وتاريخ Git تبقى جزءاً من قبول الإصدار. |

## ما لا يثبت هذا الإصدار

لا يُنشئ الفحص الإنتاجي أي مريض أو حجز أو موعد أو account اختبار، ولا يشغل K6 أو stress/concurrency على Production. حجية تكامل الحجز والـsnapshot والسعر وidempotency والسباق وIDOR مستمدة من Staging المعزول فقط. كذلك لم يُثبت إرسال OTP حقيقي، لأن تهيئة Twilio Verify غير موجودة ولا يُضاف مزود مدفوع ضمن شرط الكلفة الصفرية.

## القيود والحواجز

| العنصر | الحالة المطلوبة |
|---|---|
| OTP/SMS الحقيقي | **BLOCKED** حتى يوفر المشغل تهيئة مزود الرسائل ثم يعاد اختبارها بموافقة ونطاق آمن. لا يعتبر `phone_verified_at` الاصطناعي في Staging إثباتًا لرسالة حقيقية. أزيلت SMS من **إشعارات الحجز** فقط واستبدلت بإشعار `in_app`؛ لم يُلغ حاجز OTP من الحجز. |
| واجهة العميل التشغيلي | **PASS في Preview Staging**: أُنشئ حساب TEST_STG عبر التسجيل الحقيقي، وربط بعضوية receptionist مقيّدة بالفرع و`clinic_bookings_only`. بعد إصلاح RLS رأى الفرع Alpha فقط وحجزين مقيدين، وأثبتت الواجهة تأكيد الحجز والحضور والإكمال، وأعاد الحارس كل المسارات غير المصرح بها إلى `/clinic/bookings`. لم يُكشف أي سر أو PII في التوثيق، وأزيل الحساب والعضوية والعيادتان والفروع والحجوزات والتاريخ والحضور والإشعارات التابعة جميعًا بعدادات صفرية موسعة. |
| K6 booking/retry | **BLOCKED — لا تنفيذ كتابي**: السيناريو وحراسه المحليون موجودون ويشترطون `TARGET_ENV=staging` وHTTPS وfixture خاصًا. رفض التشغيل قبل أي طلب HTTP لأن fixture غير موجودة، والفحص الحي لـStaging أعاد صفر عيادات/فروع/كتالوج/عروض/مواعيد نشطة قابلة للحجز. لم تُختلق verification أو activation لتجاوز الحارس؛ لذلك لا يوجد ادعاء تحمل أو سعة 50,000 مستخدم. |
| المواعيد التشغيلية | يتطلب تنفيذ K6 الفعلي إدخال/نشر عيادات وعروض ومواعيد **حقيقية ومتحققًا منها من المشغل المخول** في Staging أولاً، ثم إنشاء fixture TEST_STG منعزل وتنظيفه. لا تُنشأ بيانات وهمية أو تفعيلات غير متحققة في Production. |
| حماية كلمات المرور المسرّبة | **WARN** من Supabase فقط؛ تحسين متاح في خطة أعلى ولا تُفعّل ترقية أو شراء تلقائيًا. |

## قواعد الرجوع والتشغيل الآمن

إذا ظهر فشل حرج بعد هذا الإصدار، يُعاد توجيه Production إلى deployment السابق `dpl_2HXL8qSfRKfCXJzaaP8hZdZFp8jt` ثم يُعالج المصدر في فرع، ويُختبر في Staging قبل دفع `main` جديد. لا تستخدم rollback للنشر كبديل لعكس DDL: ترحيلات المخطط الحالية تحتاج migration مصدرية أمامية صريحة إذا تطلب الرجوع تغييرًا في قاعدة البيانات.

لا تضع مفاتيح أو JWT أو كلمات مرور أو dumps في Git أو الوثائق أو logs. لا تُطبق DDL مباشرةً خارج migration مصدرية، ولا تجرّب الحجز/OTP أو الضغط في Production.

## المراجع

| المرجع | الغرض |
|---|---|
| [README الجذري](../README.md) | المصدر الهندسي وعقود المسارات والأمن والتشغيل. |
| [سجل الترحيلات المطبقة](../supabase/REMOTE_APPLIED_MIGRATIONS.md) | المطابقة الفعلية لترحيلات Production. |
| [دليل النشر](DEPLOYMENT_RUNBOOK.md) | تسلسل النشر والقبول والرجوع. |
| [فهرس المصادر](SOURCE_MANIFEST.md) | خريطة الملفات المهمة للمطورين. |
| [README لـbaseline](../supabase/baselines/README.md) | بناء مخطط Staging المعزول وتسلسل التقوية. |
