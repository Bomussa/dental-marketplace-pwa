# الحالة الإنتاجية الحالية — أسناني قطر / MMC-MMS

**آخر مراجعة موثقة:** 26 أغسطس 2026. آخر **إصدار وظيفي** Production موثق هو `ff7d837627d20d65481c00de92faff9d4d916fdf` على `main`، ونشر Vercel الإنتاجي المطابق `dpl_E4xLiq59wYkiDTJevmHi7GPW6PPf` بحالة `READY`. تحمل السلف السابقة تغييرات التطبيق وقابلية إعادة البناء وتقوية RPC؛ وقد طُبق تسلسل حصر RPC في مشروع Supabase الإنتاجي `bqvcukxfsnchvkgejolz`. يقرأ هذا الملف مع [README الجذري](../README.md)، وهو **سجل الإصدار والحالة الحالية**؛ أما التقارير المؤرخة الأقدم فتبقى أدلة تاريخية ضمن نطاقها الزمني.

> **حكم البوابة الحالي:** **NOT READY — RELEASE BLOCKERS REMAIN.** نُشرت تقوية الصلاحيات والإصلاحات المثبتة بنجاح، وثُبت مسار العميل التشغيلي في رحلة Browser مستقلة على Preview Staging ثم نُظفت بياناتها. لكن لا يجوز إعلان جاهزية حجز عامة كاملة ما دام OTP/SMS الحقيقي غير مهيأ، ولم ينفذ اختبار K6 الفعلي للحجز/إعادة المحاولة بسبب خلو Staging من أي عيادة أو فرع أو كتالوج أو عرض أو موعد منشور وقابل للحجز. لا تُنشأ تفعيلات أو مواعيد اصطناعية لتجاوز ضوابط التحقق، ولا تُنشأ بيانات تشغيلية اصطناعية في Production.

## سجل الإصدار

| البند | القيمة المثبتة |
|---|---|
| المستودع والفرع | `Bomussa/dental-marketplace-pwa`، الفرع `main`. |
| آخر Commit وظيفي منشور | `ff7d837627d20d65481c00de92faff9d4d916fdf` — توثيق مواءمة هوية Production؛ يتضمن السلف `8449ab093c07a4f750b3d6a69572423e54dbc32c` الذي يحمل تغييرات التطبيق وقابلية إعادة البناء. |
| Vercel | المشروع `dental-marketplace-pwa` (`prj_Dj3iqScGPVpMZdbluhw7YUwNBbSr`)؛ آخر نشر Production وظيفي `dpl_E4xLiq59wYkiDTJevmHi7GPW6PPf` بحالة `READY` ويطابق `ff7d837`. |
| النطاقات | [www.mmc-mms.com](https://www.mmc-mms.com) و[mmc-mms.com](https://mmc-mms.com). |
| Supabase Production | `bqvcukxfsnchvkgejolz`؛ طابق عداد الكتالوج الصفحة العامة: 48 علاجًا رئيسيًا و79 نوعًا دقيقًا. |
| Staging المنفصل | `yrlwoxlxizxgrodtbdcp`؛ استُخدم لاختبارات الكتابة والـfixtures ثم نُظف، ولم تُنقل أي بيانات منه إلى Production. |
| هدف الرجوع | نشر Production السابق `dpl_65J1617gNn3neDYr2c4ragYpHety`، المرتبط بـ`9497d6fc6f88aa2349eaa659c0769c992c074bd4`، متاح كـrollback candidate. |

## التغييرات المنشورة ولماذا

| التغيير | السبب والأثر | حالة التحقق |
|---|---|---|
| حصر تنفيذ RPC | سُحب تنفيذ كل دوال `public` من `PUBLIC` و`anon` و`authenticated`، ثم أُعيد منح أقل صلاحيات لدوال الواجهة المقصودة فقط. الدوال ذات اللاحقة `_server` بقيت لـ`service_role` فقط. | **PASS**: بعد التطبيق كان `anon_server_execute=0` و`authenticated_server_execute=0` و`service_server_execute=23`. |
| البحث العام والموثق | بقي `search_dental_offers` ومساعد نطاق السعر غير الخادمي متاحين للزوار، وبقي البحث نفسه متاحًا للمستخدم المسجل. | **PASS**: تحقق grants حي وصفحة `/results` عامة ناجحة بلا كتابة. |
| قابلية إعادة بناء المخطط | أضيف baseline خالٍ من البيانات مع مولدي/متحقق كتالوج، ووثق تسلسل baseline ثم مهاجرات التقوية الخمس. | **PASS في Staging محلي معزول**: تطابق 49 جدولًا و75 دالة و321 قيدًا وRLS/policies/indexes/triggers المرجعية، مع postconditions للتقوية. |
| cache كتالوج العلاج | قُصّرت إعادة التحقق إلى 60 ثانية وأُبطل مفتاح cache السابق كي لا تبقى بيانات fixtures المحذوفة ظاهرة في Preview. | **PASS** في Preview Staging بعد تنظيف TEST_STG. |
| اختبار اتصال Supabase المحلي | أصبح اختبار الاتصال الخارجي مشروطًا بتوفر متغيري public الفعليين؛ لا يضع أي مفتاح في shell أو Git، لكنه يبقى فعالًا عند توفرهما. | **PASS**: `npm run verify` ينجح مع تخطي الاختبار غير المهيأ محليًا. |

## ترحيلات Production المطبقة في هذا الإصدار

لا تتضمن هذه الترحيلات حذف بيانات أو نقلًا بين البيئات أو إنشاء حسابات/حجوزات/مواعيد. تحفظ الملفات المصدرية ضمن `supabase/migrations/`، وقد سجلتها Supabase بالأرقام التالية عند التطبيق في Production.

| ملف المصدر | الإصدار المسجل في Production | الغرض |
|---|---:|---|
| `20260825191000_harden_public_function_execute.sql` | `20260825201107` | حصر تنفيذ الدوال وخدمات RPC الخادمية. |
| `20260825192500_revoke_anon_server_rpc_execute.sql` | `20260825201142` | إزالة منح anon الموروثة وإبقاء بحث الزوار. |
| `20260825193500_limit_authenticated_security_definer_rpc.sql` | `20260825201217` | منح authenticated لدوال الواجهة المحددة فقط. |
| `20260825194500_allow_public_search_validation_helper.sql` | `20260825201253` | منح مساعد نطاق السعر غير الخادمي للبحث العام فقط. |
| `20260825195000_allow_authenticated_search_rpc.sql` | `20260825201329` | إبقاء بحث القراءة فقط متاحًا للمستخدم المسجل. |

## التحقق الفعلي

| المجال | النتيجة |
|---|---|
| فحص المصدر | `npm run verify`: فحص predeploy وTypeScript والبناء نجحت؛ 90 اختبار Vitest نجح واختبار اتصال public واحد تُخطي لغياب إعداد محلي؛ تحذيران lint سابقان فقط عن default exports مجهولة في ملفات K6. |
| اختبار Browser المحلي وStaging | **BLOCKED محليًا**: `npm run test:e2e` يحتاج URL ومفتاح Supabase public محليين؛ لم تُنسخ قيم Production أو Staging إلى shell. **PASS في Preview Staging**: تسجيل دخول العميل التشغيلي عبر واجهة `/login` أصدر JWT بالنطاق `clinic_bookings_only`، ووصل إلى `/clinic/bookings` مع فرع مخوّل وحالة صفرية آمنة. أعادت محاولات `/` و`/account` و`/admin` و`/clinic` إلى مساحة الحجوزات وفق حاجز proxy المركزي. نُظفت جميع fixtures بعد ذلك. |
| صفحة Production الرئيسية | **PASS**: HTTPS والواجهة العربية والكتالوج العام حملا بنجاح بعد النشر، مع 48/79 من بيانات Production القائمة. |
| البحث والنتائج | **PASS**: صفحة نتائج عامة لنوع علاجي قائم استجابت بلا خطأ صلاحيات؛ عدم وجود نتيجة لهذا النوع لا يمثل خللًا بحد ذاته ولا يبرر إنشاء موعد اصطناعي. |
| الصحة | **PASS**: `/api/health` أعاد فقط `{ "ok": true, "time": ... }` بعد النشر. |
| Console | **PASS**: لا توجد رسائل Console في الفحص النهائي. |
| أخطاء تشغيل Vercel | **PASS للإصدار الجديد**: لا توجد مجموعة خطأ مرتبطة بالنشر الجديد؛ ظهرت مجموعة قديمة واحدة فقط لمسار تثبيت الجهاز آخرها قبل النشر الجديد. |
| مستشار الأمان | **PASS مع تحذير خطة**: لا تحذيرات SECURITY DEFINER أو grants عامة بعد التقوية؛ بقي فقط `auth_leaked_password_protection`، وهو يتطلب خطة أعلى ولم تُنشأ ترقية أو تكلفة تلقائية. |
| مستشار الأداء | **INFO فقط**: مؤشرات فهارس غير مستخدمة بعد؛ لم تُحذف من Production لأن عدم الاستخدام المرصود لا يثبت عدم لزومها تشغيليًا. [إرشاد Supabase](https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index). |

## ما لا يثبت هذا الإصدار

لا يُنشئ الفحص الإنتاجي أي مريض أو حجز أو موعد أو account اختبار، ولا يشغل K6 أو stress/concurrency على Production. حجية تكامل الحجز والـsnapshot والسعر وidempotency والسباق وIDOR مستمدة من Staging المعزول فقط. كذلك لم يُثبت إرسال OTP حقيقي، لأن تهيئة Twilio Verify غير موجودة ولا يُضاف مزود مدفوع ضمن شرط الكلفة الصفرية.

## القيود والحواجز

| العنصر | الحالة المطلوبة |
|---|---|
| OTP/SMS الحقيقي | **BLOCKED** حتى يوفر المشغل تهيئة مزود الرسائل ثم يعاد اختبارها بموافقة ونطاق آمن. لا يعتبر `phone_verified_at` الاصطناعي في Staging إثباتًا لرسالة حقيقية. |
| واجهة العميل التشغيلي | **PASS في Preview Staging**: أُنشئ حساب TEST_STG عبر التسجيل الحقيقي، وربط بعضوية receptionist مقيّدة بالفرع و`clinic_bookings_only`، ثم سجل الدخول من الواجهة ووصل إلى `/clinic/bookings`. لم يُكشف أي سر أو PII في التوثيق، وأزيل الحساب والعضوية والعيادة والفرع والآثار التابعة جميعًا. |
| K6 booking/retry | **BLOCKED — لا تنفيذ كتابي**: السيناريو وحراسه المحليون موجودون ويشترطون `TARGET_ENV=staging` وHTTPS وfixture خاصًا. رفض التشغيل قبل أي طلب HTTP لأن fixture غير موجودة، والفحص الحي لـStaging أعاد صفر عيادات/فروع/كتالوج/عروض/مواعيد نشطة قابلة للحجز. لم تُختلق verification أو activation لتجاوز الحارس؛ لذلك لا يوجد ادعاء تحمل أو سعة 50,000 مستخدم. |
| المواعيد التشغيلية | يتطلب تنفيذ K6 الفعلي إدخال/نشر عيادات وعروض ومواعيد **حقيقية ومتحققًا منها من المشغل المخول** في Staging أولاً، ثم إنشاء fixture TEST_STG منعزل وتنظيفه. لا تُنشأ بيانات وهمية أو تفعيلات غير متحققة في Production. |
| حماية كلمات المرور المسرّبة | **WARN** من Supabase فقط؛ تحسين متاح في خطة أعلى ولا تُفعّل ترقية أو شراء تلقائيًا. |

## قواعد الرجوع والتشغيل الآمن

إذا ظهر فشل حرج بعد هذا الإصدار، يُعاد توجيه Production إلى deployment السابق `dpl_65J1617gNn3neDYr2c4ragYpHety` ثم يُعالج المصدر في فرع، ويُختبر في Staging قبل دفع `main` جديد. لا تستخدم rollback للنشر كبديل لعكس DDL: مهاجرات grants الحالية قابلة للمراجعة وتحتاج migration مصدرية صريحة إذا تطلب الرجوع تغيير صلاحيات قاعدة البيانات.

لا تضع مفاتيح أو JWT أو كلمات مرور أو dumps في Git أو الوثائق أو logs. لا تُطبق DDL مباشرةً خارج migration مصدرية، ولا تجرّب الحجز/OTP أو الضغط في Production.

## المراجع

| المرجع | الغرض |
|---|---|
| [README الجذري](../README.md) | المصدر الهندسي وعقود المسارات والأمن والتشغيل. |
| [سجل الترحيلات المطبقة](../supabase/REMOTE_APPLIED_MIGRATIONS.md) | المطابقة الفعلية لترحيلات Production. |
| [دليل النشر](DEPLOYMENT_RUNBOOK.md) | تسلسل النشر والقبول والرجوع. |
| [فهرس المصادر](SOURCE_MANIFEST.md) | خريطة الملفات المهمة للمطورين. |
| [README لـbaseline](../supabase/baselines/README.md) | بناء مخطط Staging المعزول وتسلسل التقوية. |
