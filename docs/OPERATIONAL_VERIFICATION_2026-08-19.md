# التحقق التشغيلي الحي — 19 أغسطس 2026

> **النطاق:** فحص قراءة فقط للموقع العام ونقاطه المسموح بها، مع مراجعة حالة نشر Vercel وسجل الأخطاء. لم تُنشأ حجوزات أو رسائل SMS أو بيانات مريض أو تغييرات تشغيلية. هذا تقرير زمني مكمل لـ[الحالة الإنتاجية الحالية](CURRENT_PRODUCTION_STATUS.md)، وليس بديلاً عنها.

## النتيجة التنفيذية

تؤكد الجولة أن النطاقين يعملان، وأن أحدث إيداع توثيقي `4f9859a` وصل إلى نشر إنتاجي بحالة `READY`. لم تظهر مجموعات أخطاء runtime في Vercel خلال نافذة 24 ساعة التي فُحصت، وعادت الطلبات العامة غير المخولة أو غير الصحيحة بالرفض المتوقع قبل أي كتابة. اكتشف الفحص خطأً محدوداً في **معرف فريق Vercel المدوّن في دليل النشر**؛ صحح في الإيداع اللاحق بهذا التقرير.

| المحور | النتيجة | نوع التحقق |
|---|---|---|
| صحة الخدمة | `/api/health` أعاد `ok=true` و`database=true` و`server_operations=true` و48 علاجاً رئيسياً. | طلب قراءة عام. |
| النطاقات | أعاد `mmc-mms.com` تحويل HTTPS من نوع `308` إلى `www.mmc-mms.com`، وأعاد الأصلان نقطة الصحة السليمة. | رؤوس وقراءة عامة. |
| الواجهة العامة | ظهرت العربية والإنجليزية وخيارات العلاج والنوع والموعد ونطاق المسافة 1/5/10/25/50 كم ومعايير الفرز. | فحص بصري بلا إرسال نموذج. |
| الحماية | أعاد البحث غير الصالح `400`، والحجز بلا جلسة `401`، وكتابة تثبيت جهاز من أصل خارجي `403`، وتصدير نشاط الإدارة بلا صلاحية `403`. | طلبات رفض لا تنفذ كتابة. |
| رؤوس المتصفح | ظهرت HSTS وCSP و`X-Frame-Options: DENY` و`nosniff` و`Referrer-Policy` المقيد، مع `no-store` لنقطة الصحة. | رؤوس HTTP عامة. |
| تبعيات الإنتاج | `npm audit --omit=dev` أعاد 0 ثغرات معروفة. | فحص محلي للنسخة الحالية. |
| Vercel | الإيداع `4f9859a` هو أحدث نشر إنتاجي بحالة `READY`؛ لم تظهر أخطاء runtime في 24 ساعة. | مراقبة استضافة للقراءة فقط. |

## التصحيح المنفذ

| المشكلة المثبتة | الأثر | المعالجة |
|---|---|---|
| كان دليل النشر يذكر `team_aFtFTvzgabqEN5bOxn4SiO7`. | قد تفشل أوامر مراقبة Vercel وقراءتها بحالة عدم تخويل. | استبدل بالمعرف المصرح به الذي أعادته منصة الاستضافة: `team_aFtFTvzgabqENB5bOxn4SiO7`. |

## ملاحظة أداء مسؤولة

نجحت القراءات الخفيفة، لكن الزمن الكلي المرصود من بيئة التحقق تراوح تقريباً بين 4.02 و5.71 ثوانٍ، وظهر جزء معتبر منه في TLS/الشبكة. لا يثبت ذلك سبباً في منطق التطبيق ولا يصلح لادعاء زمن استجابة ثابت. لم تُظهر مراقبة Vercel أخطاء runtime، لكن الحكم على الأداء يتطلب مراقبة متعددة المناطق أو بيانات تحليلات استضافة مفصلة قبل نسبة التأخر إلى التطبيق أو اعتماده كهدف أداء.

## حدود التقرير

لا يتضمن هذا التقرير اختبار ضغط، ولا يفترض جاهزية تحميل آلاف أو ملايين الجلسات. كما لا يختبر نجاح تسليم SMS أو تحديثاً حيّاً يحتاج جلسات متعددة. تبقى هذه اختبارات مقصودة ومقيدة تحتاج حسابات اختبار آمنة، ومزوّد رسائل مهيأ، وخطة حمل لا تؤثر في المستخدمين أو البيانات الحقيقية.

## المراجع

| المرجع | الاستخدام |
|---|---|
| [README الجذري](../README.md) | العقود والبنية وضوابط الأمن. |
| [الحالة الإنتاجية الحالية](CURRENT_PRODUCTION_STATUS.md) | موجز الحالة والقيود الحالية. |
| [دليل النشر](DEPLOYMENT_RUNBOOK.md) | متغيرات التشغيل ومسار الإصدار ومعرف الفريق المصحح. |
| [تقرير الاختبارات الحالي](TEST_REPORT.md) | بوابة الجودة الآلية ونطاقها. |
