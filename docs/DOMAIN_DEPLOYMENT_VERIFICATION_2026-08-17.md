# تحقق النطاق والنشر — 17 أغسطس 2026

| البند | النتيجة |
|---|---|
| مشروع Vercel | `dental-marketplace-pwa` في حالة نشر إنتاجي `READY` للالتزام `b63d964`. |
| النطاقات المربوطة | `mmc-mms.com` و`www.mmc-mms.com` كلاهما مدرجان ضمن نطاقات المشروع. |
| `https://mmc-mms.com/` | يعيد `308` إلى `https://www.mmc-mms.com/`، وهو النطاق الكنسي المقصود. |
| `https://www.mmc-mms.com/` | يعيد `200` ويحمّل واجهة التطبيق في المتصفح دون خطأ شهادة. |
| HTTPS | كلا النطاقين يقدمان `Strict-Transport-Security: max-age=63072000`. |
| الصحة | `https://www.mmc-mms.com/api/health` يعيد `200`؛ النطاق الجذري يلتزم بإعادة التوجيه الكنسية. |

> لا يلزم إعادة نشر أو تعديل DNS: النسخة الإنتاجية الحالية مربوطة بالنطاقين بالفعل، والنطاق الجذري يعيد توجيهًا آمنًا إلى `www` لتفادي تكرار المحتوى.
