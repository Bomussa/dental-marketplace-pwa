# معاينة PR #6 — دليل حالة النشر

- تاريخ الفحص: 15 أغسطس 2026.
- طلب الدمج: `https://github.com/Bomussa/dental-marketplace-pwa/pull/6`، وحالته مفتوح وقابل للدمج.
- صفحة GitHub سجلت أن معاينات سابقة لـVercel حُجبت بسبب عدم تمكن Vercel من التحقق من مالك البريد الإلكتروني لالتزام سابق `bf722c6` وربط وصوله بفريق النشر.
- لوحة Vercel المتصلة ظهرت مسجلة الدخول إلى فريق `bomousa-mmc`، وأظهرت معاينة PR #6 الجديدة بالحالة `Building` والرابط المتوقع: `https://dental-marketplace-pwa-git-audit-production-read-87fe06-bomussa.vercel.app`.
- لم يُجرَ أي تعديل على إعدادات حساب أو فريق أو هوية خارجية ضمن هذا الفحص.

بعد ظهور حالة `Building` في لوحة Vercel، أصبح رابط المعاينة المباشر قابلاً للوصول بنجاح. عُرض عنوان «أسناني قطر» مع التنقل العربي وعناصر البحث وزر اللغة وواجهة الدعم، ما يثبت نجاح تجميع معاينة PR #6 ووصول الصفحة العامة دون بيانات اختبار ظاهرة.

أعادت نقطة الصحة في المعاينة `{"ok":true,"database":true,"treatments":25}`. كما أعادت طلبات POST غير الموثقة إلى `/api/book` و`/api/support` حالة HTTP `401`، ما يثبت تطبيق حارس المصادقة في النشر الفعلي قبل أي عملية كتابة أو استدعاء دعم.

في اختبار واجهة الجمهور الحي، ظهرت الصفحة أولًا بالعربية مع اتجاه RTL ونصوص البحث والدعم. أدى زر اللغة إلى تحويل الواجهة إلى الإنجليزية فورًا، بما يشمل العنوان والتنقل وحقول البحث والمؤشرات وواجهة الدعم، مع بقاء زر الرجوع إلى العربية متاحًا.

اكتمل مسار البحث في المعاينة من الصفحة الرئيسية إلى النتائج بالمعايير المختارة. وعند عدم وجود عرض مؤهل، عرضت الصفحة حالة صفر نتائج ورسالة تشرح أن ذلك لا يعني غياب الخدمة في قطر، بدل عرض مواعيد أو أسعار وهمية.

أظهر فحص إعدادات مشروع `dental-marketplace-pwa` في Vercel ثلاثة متغيرات فقط: `NEXT_PUBLIC_SITE_URL` و`NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY` و`NEXT_PUBLIC_SUPABASE_URL`. لا يظهر `SUPABASE_SERVICE_ROLE_KEY` ضمن إعدادات الإنتاج أو المعاينة؛ لذلك تبقى العمليات الخادمية المحصورة، مثل الحجز وتسجيل الجهاز وعمليات العيادة والإدارة، غير قابلة للتشغيل في النشر حتى إضافة السر المطلوب.

بعد طلب التحقق من الإضافة، ظلت قائمة المتغيرات العامة تعرض ثلاثة متغيرات فقط لمشروع المنصة. سيجري الفحص من صفحة المشروع المحددة مباشرة؛ لا تكفي القائمة العامة للحكم النهائي إذا كان التحديث لم ينعكس بعد.

ظهر `SUPABASE_SERVICE_ROLE_KEY` في إعدادات متغيرات مشروع `dental-marketplace-pwa` مباشرة بعد الإضافة. ستلتقطه معاينة جديدة ناشئة من التزام لاحق؛ لا تُسجل قيمة السر في المستودع أو التقرير.

## تحقق الإنتاج بعد الدمج

في فحص النطاق الرسمي `www.mmc-mms.com`، أعادت نقطة الصحة حالة نجاح مع اتصال قاعدة البيانات و25 علاجًا أساسيًا. كما عُرضت الواجهة العربية العامة كاملة على النطاق الرسمي دون ظهور بيانات اختبار للعملاء.
أعاد اختبار محدود لمسار تسجيل الجهاز على النطاق الرسمي حالة `503` برسالة حماية واضحة قبل أي كتابة، رغم نجاح نقطة الصحة. يثبت ذلك أن مفتاح الخدمة المضاف غير صالح لمشروع قاعدة البيانات أو لم يلتقطه نشر الإنتاج الصحيح بعد الاستبدال.

تم تسجيل الدخول إلى لوحة Supabase وتأكيد أن مشروع `qatar-dental-dev` المتصل يعرض واجهة Data API بعنوان المشروع `bqvcukxfsnchvkgejolz`. لم تُعرض أو تُنسخ أي قيمة سرية ضمن هذا التحقق.
أظهرت معاينة الالتزام التشخيصي `5350450` صفحة «Deployment is building»، لذا كانت حالة القائمة المؤقتة لا تعني فشل بناء نهائيًا. سيؤجل اختبار مسار RPC إلى أن تصبح المعاينة جاهزة.
أظهر Vercel أن حجب المعاينات كان ناتجًا عن صيغة بريد مؤلف GitHub غير المطابقة للحساب. صُححت الصيغة إلى العنوان المرتبط بالحساب، وأصبحت معاينة الالتزام `332dc81` متاحة بنجاح على النطاق الفريد مع الواجهة العامة الكاملة.
تم تشخيص رسالة `Invalid API key` من سجل Vercel، ثم استبدال `SUPABASE_SERVICE_ROLE_KEY` في Vercel بمفتاح سري بصيغة صحيحة من مشروع `qatar-dental-dev` دون تدوين القيمة. شُغّلت إعادة نشر إنتاجية من الالتزام `c86aae2` واكتملت بحالة `Ready` مع ربط `www.mmc-mms.com`.
بعد اكتمال إعادة النشر، أعاد اختبار `POST /api/device-installations` على `www.mmc-mms.com` الحالة `200` مع `{ "ok": true }` باستخدام معرّف قبول غير شخصي. حُذف سجل الاختبار فورًا من `device_installations`، وأكدت عملية التنظيف حذف سجل واحد.


## قبول الهوية الثنائية والثيمات — المعاينة `bca185a`

اكتمل نشر معاينة الالتزام `bca185a` بحالة **Ready** على الرابط `https://dental-marketplace-pgz02f48d-bomussa.vercel.app`، وأظهرت صفحة الالتزام فحوصًا ناجحة للنشر ومراجعة المعاينة.

تم اختبار مبدل المظهر من رأس الصفحة العامة. يبدأ المبدل مغلقًا، ثم يتيح خيارات **المراجع** و**العيادة** و**الإدارة** و**تلقائي حسب الصفحة**. غيّر اختيار العيادة الخلفية والعناصر الرئيسية فورًا إلى الأخضر والذهبي، وغيّر اختيار الإدارة إلى البنفسجي والأزرق. تحقق DOM من أن اختيار الإدارة يُخزَّن محليًا ويحدث `data-theme` ووسم `theme-color` إلى `#6D45CF`، ثم أزال خيار التلقائي التفضيل المحلي وأعاد ثيم المراجع.

كما نُفّذ اختبار العربية والإنجليزية في المعاينة. ظهرت تسميات المبدل كاملة ومطابقة في اللغتين، بما فيها `Patient / Clinic / Administration / Automatic by page` وبدائلها العربية، مع استمرار تحميل بيانات كتالوج العلاج الحية ودون أخطاء متصفح مرئية أثناء الاختبار.
