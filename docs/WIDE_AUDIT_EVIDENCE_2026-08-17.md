# أدلة التدقيق الموسع — 17 أغسطس 2026

## اختبار النطاق الفعلي `www.mmc-mms.com`

| المسار | الإجراء | النتيجة المتحققة |
|---|---|---|
| `/` | فتح النطاق المخصص للإنتاج | حُمّلت واجهة عربية سليمة، مع 25 علاجًا رئيسيًا و46 نوعًا دقيقًا. |
| `/results` | إرسال البحث الافتراضي | انتقل المستخدم إلى نتائج الخدمة الصحيحة؛ الحالة الفارغة تعرض `0 نتيجة` ورسالة دقيقة بلا كشف عروض DEV صناعية. |
| `/api/health` | طلب من المتصفح | `200` مع `database: true` و`server_operations: true` و25 علاجًا. |
| `/api/search` بمدخلات غير صالحة | طلب آمن بلا تغيير | `400 invalid_search`. |
| `/api/book` بجسم فارغ | طلب آمن بلا تغيير | `401` قبل التحقق أو إنشاء أي حجز. |
| `/api/choices` بجسم فارغ | طلب آمن بلا تغيير | `400 invalid_choice_event`. |
| `/account` | زيارة زائر غير موثق | إعادة توجيه إلى `/login?next=/account`. |
| `/clinic` | زيارة زائر غير موثق | إعادة توجيه إلى `/login?next=/clinic`. |

> لم يُرسل Magic Link ولم يُنشأ حجز أو سجل تحليلي أو دعم؛ جميع طلبات الاختبار التي تمس العمليات كانت غير صالحة عمدًا أو غير موثقة، وبالتالي بلا أثر جانبي.

| `/admin` | زيارة زائر غير موثق | أعادت الصفحة إلى `/` دون عرض واجهة الإدارة؛ المصدر يؤكد أن هذا سلوك إخفاء مقصود عند غياب مطالبة `platform_admin`. |
| GitHub Actions | فحص سجل التشغيل للالتزام `f5596a0` | آخر تشغيل CI مكتمل بنجاح، كما أن تشغيلَي المصدر السابقين `f887081` و`28c5694` مكتملان بنجاح. |
| تبديل اللغة على `/` | الانتقال من العربية إلى الإنجليزية | اكتمل التحويل في الإنتاج: العنوان ونموذج البحث والكتالوج وحالة المساعد أصبحت إنجليزية، مع بقاء 25 علاجًا و46 نوعًا دقيقًا وإجراء البحث متاحًا. |
| `/sw.js` في الإنتاج | طلب مباشر من جلسة المتصفح | `200`، نوع `application/javascript`، وسياسة إعادة تحقق عامة؛ يحمل عامل الخدمة العام فقط. |
| `/manifest.webmanifest` في الإنتاج | طلب مباشر من جلسة المتصفح | `200`، نوع `application/manifest+json`، وبيان التطبيق العربي متاح. |
