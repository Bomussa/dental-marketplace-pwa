
## فحص عام قراءة فقط — 2026-08-21T07:07Z

زُارت الصفحة الرئيسية الرسمية `https://www.mmc-mms.com/` في متصفح حي دون تسجيل دخول أو كتابة أو حجز. ظهرت الهوية العربية وواجهة المقارنة: علاج رئيسي، نوع دقيق، تفضيل موعد، نطاق مسافة، معيار ترتيب، وزر «قارن الخيارات الآن»، إضافة إلى عبارة مجانية المقارنة وطلب الحجز للمريض. أظهرت الصفحة 48 علاجاً رئيسياً و79 نوعاً علاجياً دقيقاً. تم تفعيل زر تغيير اللغة؛ بقيت الصفحة متجاوبة أثناء التحميل، وتغطي اختبارات المتصفح المحلية ترجمة التجربة الإنجليزية نفسها.

لقطات الدليل المحلية: `/home/ubuntu/screenshots/mmc-mms_2026-08-21_07-07-18_3924.webp` و`/home/ubuntu/screenshots/mmc-mms_2026-08-21_07-07-27_2106.webp`.

تم التحقق لاحقاً من اكتمال تبديل اللغة إلى الإنجليزية بنجاح: تغيّرت عناوين التنقل وواجهة المقارنة والشرح وعبارة المجانية وتفاصيل الكتالوج إلى الإنجليزية، مع بقاء أزرار المقارنة والموقع والمساعدة ظاهرة ومتاحة. لقطة الدليل: `/home/ubuntu/screenshots/mmc-mms_2026-08-21_07-07-45_3310.webp`.

## صفحة الدخول — فحص قراءة فقط

زُارت `https://www.mmc-mms.com/login?next=%2Faccount` في الجلسة الحية دون إدخال بيانات. ظهرت واجهة دخول ثنائية اللغة بحسب اللغة المختارة، مع حقلي **Username** و**Password** وزر **Sign in**، ونص يوضح أن كلمة المرور لا تُخزن في سجلات المرضى أو العيادات. الرابط يحافظ على وجهة داخلية `/account` فقط. لقطة الدليل: `/home/ubuntu/screenshots/mmc-mms_2026-08-21_07-09-36_1590.webp`.

## حارس وجهة العودة المشفرة — فحص قراءة فقط

زُارت `https://www.mmc-mms.com/login?next=/%252F%252Fevil.example` مباشرةً. بقي المتصفح على نطاق `www.mmc-mms.com` وعرض صفحة الدخول المعتادة فقط؛ لم يحدث انتقال خارجي ولم تُرسل بيانات أو تُنشأ جلسة. لقطة الدليل: `/home/ubuntu/screenshots/mmc-mms_2026-08-21_07-10-03_2709.webp`.

## رحلة المقارنة العامة — فحص قراءة فقط

في `2026-08-21T08:49Z` فتحت الصفحة الرئيسية الحية باللغة الإنجليزية واخترت إجراء **Compare options now** بالاختيارات الافتراضية فقط؛ لم يُنشأ حجز أو ملف أو طلب. انتقل التطبيق إلى رابط نتائج يحتوي على العلاج والنوع الدقيق وتفضيل الموعد ونطاق 10 كم ومعيار التوازن. عرضت الواجهة حالة عدم وجود خيارات مطابقة حالياً لهذا النوع المحدد (`In-Office Whitening`) مع نص صريح لا يعمم عدم توفر الخدمة في قطر. هذه نتيجة رحلة قراءة عامة لنوع علاجي محدد، وليست اختباراً لسعة البحث أو إثباتاً لغياب كل عروض المنصة. لقطات الدليل: `/home/ubuntu/screenshots/mmc-mms_2026-08-21_08-49-23_1672.webp` و`/home/ubuntu/screenshots/mmc-mms_2026-08-21_08-49-37_2248.webp`.

### تفسير نتيجة عدم وجود خيارات

بعد نتيجة البحث العامة، نُفذ تشخيص **قراءة فقط** محدود على الإنتاج. وُجدت 13 عروض نشطة لنوع علاج العصب المفحوص؛ 10 منها تحمل نطاق سعر منشوراً ومؤكداً، لكن كل العروض الـ13 مرتبطة بعيادات موسومة `is_synthetic = true`. دالة البحث العامة تستبعد العيادات التجريبية صراحةً (`not c.is_synthetic`)؛ لذلك لا يمكن ولا ينبغي إظهار هذه الأسعار أو العروض المصطنعة للمريض في الموقع العام. عدم ظهور نتائج في الفحص الحي لا يدل على خلل في الفرز أو تكرار، بل يثبت فصل بيانات الاختبار عن تجربة الجمهور. لا تُحوّل بيانات تجريبية إلى بيانات منشورة من دون إدخال عيادات حقيقية وموافقة تشغيلية موثقة.
