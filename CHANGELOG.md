# سجل التغييرات

يتبع هذا الملف الإصلاحات والإصدارات المؤثرة فقط. تفاصيل الحالة التشغيلية والـdeployment والـrollback في [`docs/CURRENT_PRODUCTION_STATUS.md`](docs/CURRENT_PRODUCTION_STATUS.md).

## 2026-08-26 — إغلاق فجوات الجاهزية في Staging

| البند | WHAT | WHY | الأثر | حالة الاختبار |
|---|---|---|---|---|
| بوابة الجاهزية والواجهات العامة | صُحح `production-readiness-check.mjs` ليتوافق مع `{ ok, time }` ويغطي الصفحة والـcanonical وPWA وrobots/sitemap والدخول والبحث الصحيح/الفارغ/غير الصالح والرؤوس؛ أضيف اختبار انحدار محلي له. | كانت البوابة تنتظر حقول صحة قديمة فلا تصلح كدليل قبول فعلي. | يصبح فحص ما بعد النشر قراءة فقط ولا يتطلب بيانات حساب أو حجز أو OTP. | **PASS قراءة فقط على Production** و**PASS مصدر** في الاختبار المعزول. |
| الوصول والسياسات العامة | أضيف `@axe-core/playwright` لفحص WCAG A/AA، ومسارا `/privacy` و`/terms` وروابطهما في التذييل والتسجيل. | كشف الفجوة التقنية للمسارات القانونية وفحص الوصول الآلي من دون اختراع نص قانوني. | تعرض الصفحات صراحة أن النص القانوني يحتاج اعتماد المالك/المختص؛ لا تمثل شروطاً أو سياسة نافذة بعد. | **PASS تقني في Preview Staging**؛ E2E الخارجي يحتاج بيئة مصرحاً بها ويظل `IMPLEMENTED` لا PASS تشغيلياً. |
| retry والسباق في K6 | أضيف `booking-integrity-flow.js` المحروس لإعادة الطلب بنفس مفتاح idempotency أو تنافس حسابات مختلفة على slot واحد، وأزيلت تحذيرات lint من سيناريوهات K6 القائمة. | كانت تغطية K6 حجزاً مفرداً فقط ولا تثبت reconciliation أو التعارض. | لا يقبل التشغيل إلا في Staging مع HTTPS وfixtures وcookies قصيرة العمر وحراس تأكيد صريحة. | **IMPLEMENTED/BLOCKED**: Staging خالية من عناصر حجز نشطة وOTP الحقيقي غير مهيأ، لذلك لم يرسل الاختبار أي طلب كتابة. |
| توحيد RLS لملفات المرضى | أضيف `20260826230000_consolidate_patient_profiles_select_policy_v1.sql` لإزالة سياستي SELECT permissive المتداخلتين وتقييد القراءة الذاتية بملف غير مؤرشف، مع إبقاء وصول موظفي الفرع للحجز المرتبط. | كشف مستشار الأداء أن السياسة الأقدم أبقت وصولاً ذاتياً أوسع من المقصود وتضاعف تقييم RLS. | يمنع الحساب المؤرشف من استعادة قراءة ملفه الذاتي ويحسن مسار السياسة من دون فتح anon/public. | **PASS في Staging ثم Production** عبر postconditions؛ سجلت Production النسخة `20260827000621` واختفى تحذير `multiple_permissive_policies`. لا تتضمن migration أي DML أو بيانات اختبار. |

> لا تتضمن هذه الجولة أي DML أعمال أو حسابات أو حجوزات أو OTP أو K6 في Production، ولا تفعّل Twilio أو خدمة مدفوعة أو نصاً قانونياً جوهرياً.

## 2026-08-26 — إصلاح قراءة حجوزات العميل التشغيلي في Staging

| البند | WHAT | WHY | الأثر | حالة الاختبار |
|---|---|---|---|---|
| حلقة RLS بين الحجز وملف المريض | أضيف الحارس الداخلي `private.has_active_self_patient_profile` ثم أعيد تعريف `bookings_select` لاستدعائه بدلاً من استعلام `patient_profiles` مباشرة تحت RLS، مع الإبقاء على مسار فرع العيادة والمدير الأعلى كما هو. | كانت سياسة `bookings_select` تستعلم `patient_profiles` بينما سياسة الملفات التشغيلية تستعلم `bookings`، فأدى ذلك إلى `infinite recursion detected in policy for relation \"bookings\"` لحساب العميل التشغيلي. | تعود قراءة الحجوزات والعدادات للحساب المصرح به، مع بقاء منع قراءة تاريخ المريض بعد أرشفة ملفه الذاتي. الدالة خاصة وغير مكشوفة لـ`anon` أو `public`، ومنحت لـ`authenticated` فقط كي تعمل داخل السياسة. | **PASS في Staging فقط**: عداد RLS المقيد أعاد فرعًا واحدًا وحجزين ظاهرين، وعرضت `/clinic/bookings` الفرع Alpha فقط. نجحت انتقالات pending → confirmed → checked-in → completed عبر الواجهة ثم تحقق تنظيف TEST_STG من أصفار الآثار. |
| اختبار انحدار المصدر | أضيف `tests/booking-rls-recursion.test.ts` مع ثلاثة ترحيلات مصدرية تثبت الحارس والتنفيذ المقيد وكسر الاستعلام المباشر إلى `patient_profiles` ومسار بحث `pg_catalog` الثابت. | منع إعادة إدخال الحلقة عند تعديل سياسات حساب المريض أو الحجوزات لاحقًا. | يغطي عقد المصدر؛ فحص RLS الحي في Staging يبقى دليل التشغيل. | `npm run verify`: 100 اختبار ناجح، اختبارا اتصال متخطيان عمدًا، والبناء وTypeScript ناجحان؛ تحذيرا lint قديمان فقط في ملفات K6. |

> نُشر الإصلاح لاحقًا إلى `main` وProduction بعد تحقق Staging؛ يسجل [سجل الترحيلات المطبقة](supabase/REMOTE_APPLIED_MIGRATIONS.md) أرقام التطبيق الفعلية. لا تُنشأ بيانات اختبار في Production.


## 2026-08-26 — تحسين أداء سياسة الإشعارات

| البند | WHAT | WHY | الأثر | حالة الاختبار |
|---|---|---|---|---|
| `notification_outbox` RLS | استبدل ترحيل مصدر قراءة `auth.jwt()` المباشرة في سياسة إشعارات الحساب بـ`(select auth.jwt())`، مع الإبقاء على ملكية المستلم وحارس الحساب المعطل واستثناء المدير الأعلى. | منع إعادة تقييم claim لكل صف في قراءة الإشعارات. | لا يوسع من يرى الإشعار ولا يغير أي صف أو حجز أو قناة إشعار. | PASS في Staging ثم Production: تعريف السياسة الحي صحيح، واختفى تحذير `auth_rls_initplan` من مستشار الأداء. بقيت إشعارات فهارس غير مستخدمة على مستوى INFO فقط. |


## 2026-08-26 — تجربة المريض المنشورة إلى Production

| البند | WHAT | WHY | الأثر | حالة الاختبار |
|---|---|---|---|---|
| nickname وتفرد بيانات المريض | أضيف عقد nickname مخصص للمريض من 2–10، مع منع تكرار الهاتف في قاعدة البيانات والإبقاء على عقد أسماء دخول العيادات والحسابات القائمة دون تقليص. | منع إنشاء حسابين للمريض نفسه وتحسين وضوح اسم الدخول بلا كسر الحسابات التشغيلية القديمة. | التسجيل يعرض خطأ تحقق واضحًا غير كاشف لمالك بيانات أخرى، وتبقى الهوية والهاتف فريدين. | PASS للمخطط في Production بعد تسجيل الترحيل؛ PASS وظيفي في Staging عبر تسجيل TEST_STG وتنظيف صفري. |
| الحجز والإشعارات والموقع | أضيفت قناة `in_app` لتأكيد الحجز مع العلاج والفرع والمكان والوقت ورابط اتجاهات وتنبيه وصول قبل 30 دقيقة؛ أزيلت SMS من إشعارات الحجز فقط. | توفير تأكيد محفوظ داخل الحساب بلا تكلفة رسالة لكل حجز، مع إبقاء OTP حاجزًا أمنيًا منفصلًا. | يمكن للمريض مراجعة التأكيد المحفوظ وفتح الاتجاهات من بيانات الفرع المنشورة. | PASS في Staging بحجز مؤكد ثم تنظيف كامل؛ OTP الحقيقي غير مثبت. |
| تفضيل جنس الممارس | أضيف جنس الممارس إلى إدارة العيادة والموعد وRPC البحث وواجهة البحث. | يسمح باختيار طبيبة أو طبيب عندما تتوافر بيانات موثوقة وموعد مرتبط. | لا تعرض النتائج تفضيلًا غير متاح أو بيانات ممارس مخمّنة. | PASS في Staging بفلترة موعد لطبيبة ثم تنظيف fixture. |
| حذف الحساب المقيد | أضيف حذف ذاتي مؤكد للمريض وإجراء إداري أعلى مقيد، مع أرشفة الملف وتعطيل الوصول والإبقاء على سجل تشغيل أدنى. | منع الحذف من العيادة أو أي دور غير مخول، وحماية الحجوزات ذات المرجع التاريخي. | ينتقل المريض المحذوف إلى الدخول ولا تستمر جلسته. | PASS في Staging بالحذف الذاتي لـTEST_STG ثم تنظيف آثاره. |
| إصلاح تحميل الحساب والحجز | استبدلت القراءات التي كانت قد تعود فارغة في صفحات النتائج والحساب بقراءات خادمية مقيدة بمعرّف claim موثق. | ظهر خلل فعلي بعد التسجيل: الملف الذاتي والحجز لم يظهرا رغم وجودهما الموثق في Staging. | يظهر الملف الجاهز في زر الحجز، وتظهر الحجوزات والإشعارات للمالك فقط. | PASS في Staging بعد الإصلاح؛ `npm run verify` نجح بـ94 اختبارًا وواحد متخطى. |

> نُشر هذا العمل ضمن `7f814fc` إلى Production في deployment `dpl_3Wi8ZBNWHrT9Jh41S91fRSZxoE52` بحالة `READY`، وسجل Supabase الترحيلين تحت `20260826122625` و`20260826122705`. يبقى `c5a45be` دليل الاختبار الوظيفي المعزول في Staging فقط؛ لا يحول النشر أو postconditions المقروءة إلى إثبات لـOTP الحقيقي أو لحجز إنتاجي. انظر [`docs/CURRENT_PRODUCTION_STATUS.md`](docs/CURRENT_PRODUCTION_STATUS.md) لحكم البوابة والقيود.

## 2026-08-25 — تقوية RPC وقابلية إعادة البناء

| البند | WHAT | WHY | الأثر | حالة الاختبار |
|---|---|---|---|---|
| حصر تنفيذ RPC | سُحبت grants العامة ثم أعيد منح البحث ودوال الواجهة المحددة فقط؛ `_server` بقيت لـ`service_role`. | تقليل سطح تنفيذ RPC غير المقصود ومنع إتاحة الدوال الخادمية للأدوار العامة. | تقوية Authorization من دون تغيير بيانات الأعمال أو مسار الحجز الخادمي. | تم اختباره في Staging ثم طبق في Production، مع postconditions حيّة: 0 server RPC لـanon/authenticated. |
| استعادة البحث بأقل صلاحية | أضيف منح مساعد `is_price_scope_publishable` غير الخادمي للبحث العام، ومنح البحث للدور authenticated. | ظهرت فجوة فعلية في بحث Preview بعد الحصر الأولي. | استمر بحث الزوار والمستخدمين المسجلين من دون فتح دوال خادمية. | PASS في Preview وProduction read-only smoke. |
| baseline للمخطط | أضيف baseline خالٍ من البيانات ومولدا/متحقق مقارنة الكتالوج وتوثيق تسلسل التقوية. | جعل إعادة بناء Staging قابلة للتدقيق بدل الاعتماد على تاريخ ترحيلات غير مكتمل. | لا ينسخ بيانات أو أسرار ويعزل اختبارات المخطط. | PASS في Supabase محلي معزول. |
| cache كتالوج العلاج | مدة إعادة التحقق 60 ثانية ومفتاح cache جديد. | منع بقاء fixtures المحذوفة في Preview من cache تاريخي. | تحسن دقة عرض الكتالوج بعد تنظيف Staging. | PASS في Preview Staging بعد التنظيف. |
| اختبار اتصال Supabase | أصبح اختبار public connectivity مشروطًا بتوفر الإعدادات المحلية. | لا يجوز نسخ مفاتيح Preview أو Production إلى shell/الاختبار المحلي. | `npm run verify` لا يفشل شكليًا عندما لا توجد إعدادات اتصال محلية. | 90 اختبارًا ناجحًا وواحدًا متخطى محليًا عند غياب الإعدادات. |

## قيود معلنة

لا يدعي هذا السجل تشغيل OTP/SMS حقيقي أو اختبار K6 للحجز أو سعة 50,000 مستخدم. لا تنشأ بيانات اختبار في Production؛ تلك القيود وحكم بوابة الإصدار موثقة في [`docs/CURRENT_PRODUCTION_STATUS.md`](docs/CURRENT_PRODUCTION_STATUS.md).
